# GSC+ 遊戲供應商串接任務文件

## 📋 專案概述

這是一個**自學測試專案**,目標是串接 GSC+ 遊戲供應商的電子遊戲 API,實現**單一錢包 (Seamless Wallet)** 系統。

---

## 🎯 專案背景

### 開發者情況
- 正在自學開發娛樂城前後台系統
- 使用 NestJS + PostgreSQL + TypeORM
- 本機開發環境 (Windows + VPN)
- 無正式上線需求,純測試學習

### 遊戲商資訊
- **供應商**: GSC+ (GamingSoft Connect Plus)
- **環境**: Staging 測試環境
- **Operator Code**: `T9H1`
- **Secret Key**: `GSiBLVjYwuk2BtB2VAuuo7`
- **API Base URL**: `https://staging.gsimw.com`
- **貨幣**: 測試環境使用人民幣 (CNY),正式環境改用台幣 (TWD)
- **語言**: 繁體中文 (Language Code: 1)

### API 文件
- 主要文件: `Project/backend/GSC+ Seamless Wallet API v2.0.4EN.pdf`
- 測試帳號: `Project/backend/GSI (Staging) - FM T9HL GSC+_T9H1.pdf`

---

## 🏗️ 現有系統架構

### 用戶與錢包系統
```typescript
// User Entity (src/user/user.entity.ts)
- id: number (主鍵)
- username: string (唯一,用作 member_account)
- balance: number (餘額,以「分」為單位儲存,1元 = 100分)
- agentId: number (所屬代理 ID)
- companyId: number (所屬公司 ID)
```

### 交易記錄系統
```typescript
// WalletTransaction Entity (src/wallet-transaction/wallet-transaction.entity.ts)
- 完整的交易記錄系統
- 記錄所有餘額變動
- 支援 ADD, DEDUCT, ADJUST 等操作
```

### 現有遊戲系統 (Mock Games)
```typescript
// BetTxn Entity (src/modules/mock-games/entities/bet-txn.entity.ts)
- 已有基礎的注單記錄系統
- 包含下注、結算邏輯
```

### 餘額操作
```typescript
// UserService (src/user/user.service.ts)
- updateBalance(userId, amount, transactionType) 方法
- 處理餘額增減
- 自動記錄到 wallet_transactions
```

---

## 📊 單一錢包 (Seamless Wallet) 架構

### 概念說明
玩家的錢包在**運營商(您)**這邊,遊戲商不持有玩家資金。

### API 流程

#### 1️⃣ **玩家啟動遊戲**
```
前端 → 後端 (Launch Game) → GSC+ API
                                  ↓
                           返回遊戲 URL
```

#### 2️⃣ **玩家下注**
```
玩家在遊戲內下注
        ↓
GSC+ API → 您的後端 (Withdraw API)
        ↓
檢查餘額 → 扣款 → 返回扣款後餘額
```

#### 3️⃣ **遊戲結算**
```
遊戲結束
    ↓
GSC+ API → 您的後端 (Deposit API)
    ↓
加款 → 返回加款後餘額
```

#### 4️⃣ **注單同步**
```
GSC+ API → 您的後端 (Push Bet Data API)
    ↓
記錄注單到資料庫
```

---

## 🔌 需要實作的 API

### A. Seamless Wallet API (遊戲商呼叫您)

#### 1. Balance API (查詢餘額)
```
POST /v1/api/seamless/balance
```
**功能**: 遊戲商查詢玩家餘額
**處理**: 讀取 `user.balance`,轉換成元並返回

#### 2. Withdraw API (扣款/下注)
```
POST /v1/api/seamless/withdraw
```
**功能**: 玩家下注時扣款
**處理**: 
- 檢查餘額是否足夠
- 呼叫 `userService.updateBalance()` 扣款
- 記錄到 `wallet_transactions`
- 檢查交易 ID 防止重複扣款 (冪等性)

#### 3. Deposit API (入款/派彩)
```
POST /v1/api/seamless/deposit
```
**功能**: 玩家中獎時入款
**處理**:
- 呼叫 `userService.updateBalance()` 加款
- 記錄到 `wallet_transactions`
- 檢查交易 ID 防止重複入款 (冪等性)

#### 4. Push Bet Data API (注單推送)
```
POST /v1/api/seamless/pushbetdata
```
**功能**: 遊戲商推送注單資料
**處理**:
- 記錄到 `game_wagers` 表
- 不涉及餘額變動

---

### B. Operator API (您呼叫遊戲商)

#### 1. Launch Game (啟動遊戲)
```
POST https://staging.gsimw.com/api/operators/launch-game
```
**功能**: 為玩家產生遊戲啟動 URL
**參數**:
- operator_code: T9H1
- member_account: user.username
- currency: CNY (測試) / TWD (正式)
- language_code: 1 (繁體中文)
- game_code: 遊戲代碼
- product_code: 產品代碼
- platform: WEB / MOBILE
- sign: MD5 簽名

#### 2. Game List (遊戲列表)
```
GET https://staging.gsimw.com/api/operators/provider-games
```
**功能**: 取得可用遊戲列表

#### 3. Wager List (注單列表)
```
GET https://staging.gsimw.com/api/operators/wagers
```
**功能**: 查詢歷史注單

---

## 🔐 安全驗證

### MD5 簽名驗證

#### 接收遊戲商請求時 (Seamless Wallet API)
```javascript
// Balance API 簽名
sign = md5(operator_code + request_time + "getbalance" + secret_key)
例如: md5("T9H1" + "1698219740" + "getbalance" + "GSiBLVjYwuk2BtB2VAuuo7")

// Withdraw API 簽名
sign = md5(operator_code + request_time + "withdraw" + secret_key)

// Deposit API 簽名
sign = md5(operator_code + request_time + "deposit" + secret_key)

// Push Bet Data API 簽名
sign = md5(operator_code + request_time + "pushbetdata" + secret_key)
```

#### 呼叫遊戲商 API 時 (Operator API)
```javascript
// Launch Game 簽名
sign = md5(request_time + secret_key + "launchgame" + operator_code)

// Game List 簽名
sign = md5(request_time + secret_key + "gamelist" + operator_code)
```

**簽名驗證工具**: https://testcase.gscplusmd.com/

---

## 💾 資料庫設計

### 新增資料表: game_wagers (遊戲注單)

```typescript
@Entity('game_wagers')
export class GameWager {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  userId: number;  // 玩家 ID

  @Column({ nullable: true })
  agentId: number;  // 代理 ID (重要!)

  @Column()
  companyId: number;  // 公司 ID

  @Column({ unique: true })
  wagerCode: string;  // 遊戲商注單號 (唯一)

  @Column({ nullable: true })
  roundId: string;  // 遊戲局號

  @Column()
  gameCode: string;  // 遊戲代碼

  @Column()
  productCode: number;  // 產品代碼 (1006, 1009, etc.)

  @Column()
  gameType: string;  // SLOT, LIVE_CASINO, etc.

  @Column({ type: 'bigint' })
  betAmount: number;  // 下注金額 (分)

  @Column({ type: 'bigint' })
  validBetAmount: number;  // 有效投注 (分)

  @Column({ type: 'bigint', default: 0 })
  prizeAmount: number;  // 中獎金額 (分)

  @Column({ type: 'bigint', default: 0 })
  tipAmount: number;  // 小費 (分)

  @Column()
  wagerStatus: string;  // BET, SETTLED, VOID

  @Column()
  wagerType: string;  // NORMAL, FREEROUND

  @Column()
  currency: string;  // CNY, TWD

  @Column({ type: 'jsonb', nullable: true })
  payload: any;  // 遊戲商原始資料

  @Column({ type: 'bigint', nullable: true })
  settledAt: number;  // 結算時間戳 (毫秒)

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;
}
```

---

## 💰 金額處理規則

### 儲存格式
- **系統內部**: 以「分」為單位儲存 (integer)
- **範例**: 123.45 元 = 12345 分

### API 傳輸格式
- **傳給遊戲商**: 以「元」為單位,小數點後 2 位 (float)
- **範例**: 12345 分 → 123.45 (CNY 或 TWD)

### 轉換函式
```typescript
// 分 → 元
function centsToYuan(cents: number): number {
  return cents / 100;
}

// 元 → 分
function yuanToCents(yuan: number): number {
  return Math.round(yuan * 100);
}
```

---

## 🔄 交易冪等性處理

### 問題
遊戲商可能會重複發送同一筆交易 (網路問題、重試機制等)

### 解決方案
使用 `transaction.id` 或 `wager_code` 檢查是否已處理:

```typescript
// 檢查交易是否已存在
const existingTxn = await this.walletTransactionRepository.findOne({
  where: { referenceId: transaction.id }
});

if (existingTxn) {
  // 返回成功,但不重複處理
  return {
    code: 0,
    balance: user.balance / 100
  };
}

// 第一次處理,執行扣款/入款
await this.userService.updateBalance(...);
```

---

## 🚨 錯誤處理

### Seamless Wallet Code (返回給遊戲商)
```
0    - 成功
999  - 內部錯誤
1000 - 玩家不存在
1001 - 餘額不足 (重要!)
1002 - 代理金鑰錯誤
1003 - 重複交易
1004 - 簽名無效
2000 - 產品維護中
```

### 餘額不足處理
```typescript
if (user.balance < requiredAmount) {
  return {
    code: 1001,
    message: 'Insufficient balance'
  };
}
```

**絕對不允許負餘額!**

---

## 🧪 測試流程

### 階段 1: 本機開發 (目前)
1. 建立所有程式碼
2. 使用 `console.log` 測試邏輯
3. 確認沒有語法錯誤
4. **無法真正串接** (遊戲商呼叫不到 localhost)

### 階段 2: 部署到 Render.com
1. 註冊 Render 帳號 (免費)
2. 連接 GitHub repo
3. 自動部署
4. 取得固定網址: `https://your-app.onrender.com`
5. **注意**: 15 分鐘無活動會休眠,冷啟動需 30-60 秒

### 階段 3: 聯絡遊戲商
1. 提供 Callback URL:
   ```
   https://your-app.onrender.com/v1/api/seamless/balance
   https://your-app.onrender.com/v1/api/seamless/withdraw
   https://your-app.onrender.com/v1/api/seamless/deposit
   https://your-app.onrender.com/v1/api/seamless/pushbetdata
   ```
2. 遊戲商設定完成後通知您
3. 開始測試

### 階段 4: 測試步驟
1. 選擇一個測試玩家 (現有會員)
2. 用後台給玩家加值 (例如 1000 元 = 100000 分)
3. 呼叫 Launch Game API 啟動遊戲
4. 在遊戲內下注
5. 檢查後台餘額變動
6. 檢查 `game_wagers` 表的注單記錄
7. 檢查 `wallet_transactions` 表的交易記錄

---

## 📁 檔案結構

```
Project/backend/src/game-provider-gsc/
├── game-provider-gsc.module.ts
├── controllers/
│   ├── seamless-wallet.controller.ts    # Balance, Withdraw, Deposit, PushBet
│   └── operator.controller.ts           # Launch Game, Game List
├── services/
│   ├── game-provider-gsc.service.ts     # 核心業務邏輯
│   ├── signature.service.ts             # MD5 簽名驗證
│   └── operator-api.service.ts          # 呼叫遊戲商 API
├── entities/
│   └── game-wager.entity.ts             # 注單記錄
├── dto/
│   ├── balance.dto.ts
│   ├── withdraw.dto.ts
│   ├── deposit.dto.ts
│   ├── push-bet-data.dto.ts
│   └── launch-game.dto.ts
├── guards/
│   └── signature-verification.guard.ts  # API 簽名驗證
└── constants/
    └── gsc-constants.ts                 # 常數定義
```

---

## ⚙️ 重要設定

### 環境變數 (.env)
```env
# GSC+ 遊戲供應商設定
GSC_OPERATOR_CODE=T9H1
GSC_SECRET_KEY=GSiBLVjYwuk2BtB2VAuuo7
GSC_API_URL=https://staging.gsimw.com
GSC_CURRENCY=CNY  # 測試環境用 CNY,正式環境改 TWD
GSC_LANGUAGE_CODE=1  # 繁體中文
```

### VPN 需求
- **呼叫遊戲商 API 時**: 如果遊戲商有 IP 白名單,需要開 VPN (出站 IP: 185.195.61.237)
- **接收遊戲商 callback 時**: 不需要 VPN

---

## 🎮 遊戲商後台

### 登入資訊
- **URL**: https://stagingfe.gsimw.com/gsi_agent_dashboard/#/login
- **Username**: FM
- **Password**: Qwer1234

### 功能
- 查看可用遊戲列表
- 查看注單記錄
- 查看玩家餘額

---

## 📝 待辦事項清單

- [ ] 創建 game-provider-gsc module
- [ ] 建立 GameWager entity
- [ ] 實作 SignatureService (MD5 驗證)
- [ ] 實作 SignatureVerificationGuard
- [ ] 實作 Balance API
- [ ] 實作 Withdraw API (整合 UserService)
- [ ] 實作 Deposit API (整合 UserService)
- [ ] 實作 Push Bet Data API
- [ ] 實作 Launch Game API (呼叫遊戲商)
- [ ] 實作 Game List API (呼叫遊戲商)
- [ ] 本機測試邏輯 (console.log)
- [ ] 準備 Dockerfile 等部署檔案
- [ ] 部署到 Render.com
- [ ] 取得固定網址
- [ ] 聯絡遊戲商提供 callback URL
- [ ] 開始串接測試

---

## 🔗 相關連結

- GSC+ 官網: https://www.gamingsoftconnect.com
- API 文件簽名驗證工具: https://testcase.gscplusmd.com
- Render.com: https://render.com
- ngrok (備用方案): https://ngrok.com

---

## 💡 注意事項

### 1. member_account 對應
- 使用 `user.username` (不是 user.id)
- 限制 50 字元以內

### 2. 金額精度
- 系統內部以「分」儲存 (integer)
- API 傳輸以「元」為單位 (float, 保留 2 位小數)

### 3. 餘額不足
- **絕對拒絕交易**
- 返回錯誤碼 1001
- **不允許負餘額**

### 4. 代理歸屬
- 注單必須記錄 `agentId`
- 帳務算在該代理線下

### 5. 冪等性
- 使用 transaction.id 或 wager_code 檢查重複
- 重複請求返回成功但不重複處理

### 6. 測試環境限制
- 使用人民幣 (CNY)
- 正式環境會改成台幣 (TWD)
- Render 免費版會休眠 (15分鐘無活動)

---

## 📞 聯絡資訊

如有疑問,請參考:
- API 文件: `GSC+ Seamless Wallet API v2.0.4EN.pdf`
- 測試帳號文件: `GSI (Staging) - FM T9HL GSC+_T9H1.pdf`

---

**文件建立日期**: 2025-01-14  
**最後更新**: 2025-01-14  
**版本**: 1.0
